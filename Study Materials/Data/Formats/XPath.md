---
tags:
  - UPA
aliases:
  - xpath
---
A document navigation tool/technology that was originally used for XML documents, but also supported by some libraries for HTML.
Unlike the CSS it has a much more complex syntax while searching for elements, but provides more options:
- A general expression for properties of elements in `[]` that can use more values of attributes, element order and more.
- Navigation in multiple directions (axis).